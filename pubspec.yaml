name: countries_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  dio: ^4.0.0
  flutter:
    sdk: flutter
  flutter_localizations: 
    sdk: flutter
  localization: ^2.1.0
  hive:
  hive_flutter: 
  locator: ^0.0.0-dev.2
  get_it: ^7.6.0
  provider: ^6.0.5
  path_provider: ^2.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: 
  build_runner:

flutter:
  generate: true
  uses-material-design: true
  assets:
    - lib/i18n/
